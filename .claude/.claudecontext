# imas_composer - Claude Code Context

**READ THIS FILE at the start of every session working on imas_composer.**

## ⚠️ CRITICAL: Data Privacy Policy ⚠️

**NEVER EXECUTE ANY CODE IN THIS PROJECT**

This project works with restricted DIII-D fusion data that is NOT publicly accessible.

### Absolute Prohibitions:
1. **NEVER run Python code** - No python -c, no scripts, no imports, no execution whatsoever
2. **NEVER run pytest** - Tests access restricted MDS+ data
3. **NEVER execute any command** that might access MDS+ servers or data
4. **NEVER run background processes** that could fetch data

### Why:
- Running ANY code would expose restricted fusion research data
- Data access requires proper authorization that AI assistants don't have
- All data remains on user's local machine with proper access controls

### What you CAN do:
- Read and analyze code
- Write new code (but never execute it)
- Review and suggest improvements
- Help debug by reading error messages/logs provided by user
- Modify source and test files
- Use static analysis tools (Glob, Grep, etc.)

**If you catch yourself about to execute code**: STOP. The user will run it and provide you the results.

## Project Architecture at a Glance

**imas_composer** converts DIII-D MDSplus data to IMAS format using a three-stage requirement system:

1. **DIRECT**: Static MDSplus paths known at initialization
2. **DERIVED**: Requirements that depend on fetched data (e.g., channel IDs)
3. **COMPUTED**: Final composition from raw_data (no new requirements)

**Key modules:**
- `composer.py` - Requirement resolution and composition (NO data fetching)
- `fetchers.py` - Data retrieval via MDSplus/OMAS
- `ids/*.py` - IDS-specific mappers with YAML configurations

**Core principles:**
- **DRY**: All common logic in shared utilities (especially `tests/conftest.py`)
- **Requirement Isolation**: Each field fetches only what it needs
- **Declarative Config**: Field lists and static values in YAML files
- **OMAS Compatibility**: Output exactly matches OMAS reference implementation

## Quick Reference

**OMAS Integration:**
```python
machine_to_omas(ods, 'd3d', shot, 'ids_name.*')  # Fetch all fields
```

**OMAS → imas_composer mapping:**
- Static data (known paths in OMAS `query` dict) → **DIRECT** stage
- Dynamic data (depends on fetch, e.g., OMAS loops) → **DERIVED** stage
- Unit conversions must match OMAS exactly

**Development Philosophy:**
- No backwards compatibility needed - let tests fail when behavior changes
- Explicit is better than implicit
- Clear errors are better than silent fallbacks

## Documentation Index

**When to read what:**
- **Starting a session**: This file (.claudecontext) - quick orientation
- **Architecture details**: DEVELOPMENT_PRINCIPLES.md - three-stage system, DRY patterns, public API
- **Implementing fields**: IMPLEMENTING_FIELDS.md - transcription workflow from OMAS
- **Writing tests**: TESTING_GUIDE.md - test patterns and configuration
